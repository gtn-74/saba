# 作って学ぶブラウザの仕組み

## quem

QUEM は、オープンソースエミュレータ。

コンピュータシステムが別のコンピュータシステムの機能を模倣するソフトウェア or ハードウェア。

## cargo.toml

プロジェクト設定の整理管理するファイル。
ライブラリ依存とかもここに書く。（package.json 的なやつかな？）

> src ディレクトリ以下の main.rs を変更してみましょう。WasabiOS は、スタンダードライブラリに依存せずに書かれています。スタンダードライブラリとは、Rust では std によってインポートできるライブラリ群のこと

```zsh
# main.rsをバイナリにするコマンド
# x86_64-unknown-none/release/:実行ファイルが生成される。
cargo build --target x86_64-unknown-none --release
```

## OS 実行バッチ

```zsh
./run_on_wasabi.sh
```

## What is scheme?

scheme:display protocol name

## This book's lean main content

- ネットワークのやり方
- レンダリングエンジン
- js エンジン
- ui 実装

## URL の構文解析

url.rs に実装する。

## クレートとは

Rust におけるコンパイルの単位。
バイナリクレートとライブラリクレートの 2 種類が存在する。

- バイナリクレート:実行可能なバイナリを提供
- ライブラリクレート:再利用可能なコードのコレクション、モジュール化したコードを他のプログラム、クレートに提供できる。

```zsh
cargo new saba --lib
```

ライブラリクレートは単体で実行できない。ホアのプログラムの依存関係として使用する。

```zsh
cargo fmt
```
